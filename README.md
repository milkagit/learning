# Raketa static starter

## Install

```
yarn install
```

## Development

Run the SASS watcher with:

```
yarn dev
```

and then open `index.html` in your browser.

## Build for production

```
yarn build
```

This will output precompiled project into the `public` folder.
